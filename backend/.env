PORT=3001

# PostgreSQL (pg cru - DAOs existentes)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=estoque_reuso
DB_USER=postgres
DB_PASSWORD=icaro123

# Prisma ORM (Database First) - usado por `prisma db pull` / `prisma generate`
DATABASE_URL="postgresql://postgres:icaro123@localhost:5432/estoque_reuso?schema=public"

# Variabilidade LPS
EDITION=PLUS
FEATURE_RELATORIOS=true
FEATURE_USUARIOS=true
